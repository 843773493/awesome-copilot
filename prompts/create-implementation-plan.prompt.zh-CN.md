

---
agent: '代理'
description: '创建用于新功能、重构现有代码或升级包的新实施计划文件，设计、架构或基础设施。'
tools: ['变更', '搜索代码库', '编辑文件', '扩展', '获取', 'GitHub仓库', '打开简单浏览器', '问题', '运行任务', '搜索', '搜索结果', '终端最后命令', '终端选择运行', '测试失败', '用法', 'VSCode API']
---
# 创建实施计划

## 主要指令

您的目标是为 `${input:PlanPurpose}` 创建一个新的实施计划文件。输出必须是机器可读、确定性且结构化的，以便其他AI系统或人类可以自主执行。

## 执行上下文

此提示专为AI与AI之间的通信和自动化处理设计。所有指令必须严格按照字面意思进行解释和系统执行，无需人工解释或澄清。

## 核心要求

- 生成可完全由AI代理或人类执行的实施计划
- 使用无歧义的语言，无需任何解释
- 将所有内容结构化以便自动化解析和执行
- 确保文件完全自包含，无需外部依赖即可理解

## 计划结构要求

计划必须由可执行的任务组成，每个阶段必须独立处理，除非明确声明，否则不得存在跨阶段依赖。

## 阶段架构

- 每个阶段必须有可衡量的完成标准
- 阶段内的任务除非有依赖关系，否则应可并行执行
- 所有任务描述必须包含特定的文件路径、函数名称和精确的实现细节
- 任何任务都不应需要人工解释或决策

## AI优化实施标准

- 使用明确、无歧义的语言，无需任何解释
- 将所有内容结构化为机器可解析的格式（表格、列表、结构化数据）
- 在适用时包含特定文件路径、行号和精确的代码引用
- 明确定义所有变量、常量和配置值
- 在每个任务描述中提供完整上下文
- 为所有标识符使用标准化前缀（如 REQ-、TASK- 等）
- 包含可自动验证的验证标准

## 输出文件规范

- 将实施计划文件保存在 `/plan/` 目录中
- 使用命名规范：`[目的]-[组件]-[版本].md`
- 目的前缀：升级|重构|功能|数据|基础设施|流程|架构|设计
- 示例：`upgrade-system-command-4.md`，`feature-auth-module-1.md`
- 文件必须为有效的Markdown格式，包含正确的前置内容结构

## 必须遵循的模板结构

所有实施计划必须严格遵循以下模板。每个部分都是必需的，并且必须填充具体、可操作的内容。AI代理必须在执行前验证模板合规性。

## 模板验证规则

- 所有前置内容字段必须存在且格式正确
- 所有章节标题必须完全匹配（区分大小写）
- 所有标识符前缀必须遵循指定格式
- 表格必须包含所有必需的列
- 最终输出中不得包含任何占位符文本

## 状态

实施计划的状态必须在前置内容中明确定义，并反映计划的当前状态。状态可以是以下之一（状态颜色在括号中）：`已完成`（亮绿色徽章）、`进行中`（黄色徽章）、`计划中`（蓝色徽章）、`已弃用`（红色徽章）或 `暂停`（橙色徽章）。它还应在简介部分以徽章形式显示。

```md
---
目标: [简洁描述实施计划目标的标题]
版本: [可选: 例如 1.0，日期]
创建日期: [YYYY-MM-DD]
最后更新日期: [可选: YYYY-MM-DD]
负责人: [可选: 负责此规范的团队或个人]
状态: '已完成'|'进行中'|'计划中'|'已弃用'|'暂停'
标签: [可选: 相关标签或分类列表，例如 `功能`, `升级`, `日常维护`, `架构`, `迁移`, `错误` 等]
---

# 简介

![状态: <状态>](https://img.shields.io/badge/status-<状态>-<状态颜色>)

[计划的简短介绍以及其旨在实现的目标。]

## 1. 需求与约束

[明确列出影响计划的所有需求与约束，以及如何限制其实施方式。使用项目符号或表格以提高清晰度。]

- **需求-001**: 需求 1
- **安全需求-001**: 安全需求 1
- **[3字母]-001**: 其他需求 1
- **约束-001**: 约束 1
- **指导方针-001**: 指导方针 1
- **模式-001**: 需要遵循的模式 1

## 2. 实施步骤

### 实施阶段 1

- **目标-001**: [描述本阶段的目标，例如 "实现功能 X"，"重构模块 Y" 等。]

| 任务 | 描述 | 完成 | 日期 |
|------|-------------|-----------|------|
| 任务-001 | 任务 1 描述 | ✅ | 2025-04-25 |
| 任务-002 | 任务 2 描述 | |  |
| 任务-003 | 任务 3 描述 | |  |

### 实施阶段 2

- **目标-002**: [描述本阶段的目标，例如 "实现功能 X"，"重构模块 Y" 等。]

| 任务 | 描述 | 完成 | 日期 |
|------|-------------|-----------|------|
| 任务-004 | 任务 4 描述 | |  |
| 任务-005 | 任务 5 描述 | |  |
| 任务-006 | 任务 6 描述 | |  |

## 3. 替代方案

[列出任何考虑过的替代方法及其未被选择的原因。这有助于提供所选方法的上下文和理由。]

- **替代方案-001**: 替代方案 1
- **替代方案-002**: 替代方案 2

## 4. 依赖项

[列出任何需要解决的依赖项，例如库、框架或其他组件。]

- **依赖项-001**: 依赖项 1
- **依赖项-002**: 依赖项 2

## 5. 文件

[列出将受功能或重构任务影响的文件。]

- **文件-001**: 文件 1 描述
- **文件-002**: 文件 2 描述

## 6. 测试

[列出需要实现的测试，以验证功能或重构任务。]

- **测试-001**: 测试 1 描述
- **测试-002**: 测试 2 描述

## 7. 风险与假设

[列出与计划实施相关的任何风险或假设。]

- **风险-001**: 风险 1
- **假设-001**: 假设 1

## 8. 相关规范 / 进一步阅读

[链接到相关规范 1]
[链接到相关外部文档]
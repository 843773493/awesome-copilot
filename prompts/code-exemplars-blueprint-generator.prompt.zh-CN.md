

---
description: '一种与技术无关的提示生成器，用于创建可定制的AI提示，扫描代码库并识别高质量代码范例。支持多种编程语言（.NET、Java、JavaScript、TypeScript、React、Angular、Python）并可配置分析深度、分类方法和文档格式，以建立编码标准并维护开发团队之间的代码一致性。'
agent: '代理'
---

# 代码范例蓝图生成器

## 配置变量
${PROJECT_TYPE="Auto-detect|.NET|Java|JavaScript|TypeScript|React|Angular|Python|Other"} <!-- 主要技术 -->
${SCAN_DEPTH="Basic|Standard|Comprehensive"} <!-- 代码库分析的深度 -->
${INCLUDE_CODE_SNIPPETS=true|false} <!-- 是否包含实际代码片段而非仅文件引用 -->
${CATEGORIZATION="Pattern Type|Architecture Layer|File Type"} <!-- 范例的组织方式 -->
${MAX_EXAMPLES_PER_CATEGORY=3} <!-- 每个分类下的最大范例数量 -->
${INCLUDE_COMMENTS=true|false} <!-- 是否为每个范例包含解释性注释 -->

## 生成的提示

"扫描此代码库并生成一个exemplars.md文件，以识别高质量且具有代表性的代码范例。这些范例应展示我们的编码标准和模式，以帮助维护代码一致性。请使用以下方法：

### 1. 代码库分析阶段
- ${PROJECT_TYPE == "Auto-detect" ? "通过扫描文件扩展名和配置文件自动检测主要编程语言和框架" : `专注于${PROJECT_TYPE}代码文件`}
- 识别具有高质量实现、良好文档和清晰结构的文件
- 寻找常用的设计模式、架构组件和良好结构的实现
- 优先选择展示我们技术栈最佳实践的文件
- 仅引用代码库中实际存在的文件 - 不包含假设性范例

### 2. 范例识别标准
- 结构良好、可读性强且命名规范的代码
- 完整的注释和文档
- 正确的错误处理和验证
- 遵循设计模式和架构原则
- 分离关注点并符合单一职责原则
- 高效实现且无代码异味
- 代表我们标准方法的范例

### 3. 核心模式分类

${PROJECT_TYPE == ".NET" || PROJECT_TYPE == "Auto-detect" ? `#### .NET范例（如检测到）
- **领域模型**：查找正确实现封装和领域逻辑的实体
- **仓储实现**：展示我们数据访问方法的范例
- **服务层组件**：结构良好的业务逻辑实现
- **控制器模式**：干净的API控制器，包含适当的验证和响应
- **依赖注入使用**：良好的DI配置和使用范例
- **中间件组件**：自定义中间件实现
- **单元测试范例**：结构良好的测试，包含适当的设置和断言` : ""}

${(PROJECT_TYPE == "JavaScript" || PROJECT_TYPE == "TypeScript" || PROJECT_TYPE == "React" || PROJECT_TYPE == "Angular" || PROJECT_TYPE == "Auto-detect") ? `#### 前端范例（如检测到）
- **组件结构**：干净且结构良好的组件
- **状态管理**：状态处理的良好范例
- **API集成**：良好实现的服务调用和数据处理
- **表单处理**：验证和提交模式
- **路由实现**：导航和路由配置
- **UI组件**：可重用且结构良好的用户界面元素
- **单元测试范例**：组件和服务测试` : ""}

${PROJECT_TYPE == "Java" || PROJECT_TYPE == "Auto-detect" ? `#### Java范例（如检测到）
- **实体类**：良好设计的JPA实体或领域模型
- **服务实现**：干净的服务层组件
- **仓储模式**：数据访问实现
- **控制器/资源类**：API端点实现
- **配置类**：应用程序配置
- **单元测试**：良好结构的JUnit测试` : ""}

${PROJECT_TYPE == "Python" || PROJECT_TYPE == "Auto-detect" ? `#### Python范例（如检测到）
- **类定义**：结构良好且文档齐全的类
- **API路由/视图**：干净的API实现
- **数据模型**：ORM模型定义
- **服务函数**：业务逻辑实现
- **实用模块**：辅助和实用函数
- **测试用例**：结构良好的单元测试` : ""}

### 4. 架构层范例

- **展示层**：
  - 用户界面组件
  - 控制器/API端点
  - 视图模型/数据传输对象（DTO）
  
- **业务逻辑层**：
  - 服务实现
  - 业务逻辑组件
  - 工作流编排
  
- **数据访问层**：
  - 仓储实现
  - 数据模型
  - 查询模式
  
- **横切关注点**：
  - 日志实现
  - 错误处理
  - 认证/授权
  - 验证

### 5. 范例文档格式

为每个识别的范例记录：
- 文件路径（相对于仓库根目录）
- 简要描述其为何具有代表性
- 它所代表的模式或组件类型
${INCLUDE_COMMENTS ? "- 关键实现细节和编码原则" : ""}
${INCLUDE_CODE_SNIPPETS ? "- 小型、有代表性的代码片段（如适用）" : ""}

${SCAN_DEPTH == "Comprehensive" ? `### 6. 额外文档内容

- **一致性模式**：记录代码库中观察到的一致性模式
- **架构观察**：记录代码中明显的架构模式
- **实现惯例**：识别命名和结构惯例
- **应避免的反模式**：记录代码库中偏离最佳实践的区域` : ""}

### ${SCAN_DEPTH == "Comprehensive" ? "7" : "6"}. 输出格式

创建exemplars.md文件，包含：
1. 说明文档目的的简介
2. 目录，包含指向各分类的链接
3. 根据${CATEGORIZATION}组织的章节
4. 每个分类最多${MAX_EXAMPLES_PER_CATEGORY}个范例
5. 结论，包含维护代码质量的建议

该文档应为开发者提供可操作的指导，帮助其根据现有模式实现新功能。

重要提示：仅包含代码库中的实际文件。验证所有文件路径是否存在。不要包含占位符或假设性的范例。"
---

## 预期输出
运行此提示后，GitHub Copilot将扫描您的代码库并生成一个exemplars.md文件，其中包含仓库中高质量代码范例的真实引用，按照您选择的参数进行组织。